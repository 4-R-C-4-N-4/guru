// Placeholder — fully implemented in P11 (apply screen).
export function Queue(): React.ReactElement {
  return (
    <div className="mx-auto max-w-md p-8 mono text-sm text-zinc-500">
      <p>Queue + apply preview load here. Implemented in P11.</p>
    </div>
  );
}
